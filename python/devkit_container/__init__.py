"""Package data for devkit-container.

`template.Dockerfile` and `compose.template.yaml` beside this file are the Dockerfile and the
compose file `devkit setup-project` renders for a project; they ship in the same wheel as the
entrypoint binary so the three never drift apart.
"""
